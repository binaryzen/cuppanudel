// Maximum duration of a single recorded sample
export const MAX_RECORD_DURATION_MS = 30_000;

// Maximum number of samples in the media pool
export const MAX_SAMPLES = 64;

// ScriptProcessorNode buffer size for recording (ms → nearest power of 2 samples computed at runtime)
export const CHUNK_SIZE_MS = 100;
